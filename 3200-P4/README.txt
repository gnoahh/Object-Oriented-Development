This program has two objects: clusterP and dubPrime. Dubprime is an object that encapsulates a prime number and clusterP encapsulates an array of dubprime objects.

This program overrides the operators + - / * allowing special interactions between two objects clusterP and dubPrime. They can also interact with each other 
